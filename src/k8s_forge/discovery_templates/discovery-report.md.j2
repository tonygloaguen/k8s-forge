# Repository Discovery Report

This report was generated by `k8s-forge discover` from static repository analysis only.

`k8s-forge` did not execute application code, install dependencies, build images, deploy anything, contact a cluster, contact a cloud provider, create secrets, commit changes, or push Git data.

## Repository

- Path: `{{ result.repo_path }}`
- Suggested app name: `{{ result.app_name }}`
- Confidence: `{{ result.confidence }}`
- Recommended mode: `{{ result.recommended_mode }}`
- YAML generated: `{{ "yes" if result.yaml_generated else "no" }}`

## Readiness Meaning

{% if result.yaml_generated -%}
`k8s-forge-app.yaml` is a starter configuration and readiness scaffold. Review is required. It is not deployment-ready by default and does not prove Kubernetes compatibility.
{% else -%}
No `k8s-forge-app.yaml` was generated because confidence is low. Review the findings and create a configuration manually when the application shape is clear.
{% endif %}

## Files Inspected

{% for file in result.inspected_files -%}
- `{{ file }}`
{% else -%}
- No supported files were found within the static discovery limits.
{% endfor %}

## Languages

{% for language in result.languages -%}
- {{ language }}
{% endfor %}

## Frameworks

{% for framework in result.frameworks -%}
- {{ framework }}
{% else -%}
- No supported web framework confirmed.
{% endfor %}

## Dependencies

{% for dependency in result.dependencies -%}
- `{{ dependency.name }}` from `{{ dependency.source }}` ({{ dependency.category }})
{% else -%}
- No dependency metadata detected.
{% endfor %}

## Ports

{% for port in result.detected_ports -%}
- `{{ port.port }}` from {{ port.source }} (confidence: {{ port.confidence }})
{% else -%}
- No port detected.
{% endfor %}

## Startup Command

{% if result.startup_command -%}
```bash
{{ result.startup_command }}
```
{% else -%}
No reliable startup command was detected.
{% endif %}

## Environment Variables

{% for env_var in result.env_vars -%}
- `{{ env_var }}`
{% else -%}
- No environment variables detected.
{% endfor %}

## Repository Signals

- Dockerfile present: `{{ result.dockerfile_present }}`
- requirements.txt present: `{{ result.requirements_present }}`
- pyproject.toml present: `{{ result.pyproject_present }}`
- package.json present: `{{ result.package_json_present }}`
- Tests detected: `{{ result.tests_detected }}`
- CI detected: `{{ result.ci_detected }}`

## Persistence / Volume Signals

{% for volume in result.volumes_needed -%}
- {{ volume }}
{% else -%}
- No obvious local persistence signal detected.
{% endfor %}

## OS Constraints

{% for constraint in result.os_constraints -%}
- {{ constraint }}
{% else -%}
- No explicit OS constraint detected.
{% endfor %}

## Warnings

{% for warning in result.warnings -%}
- **{{ warning.code }}**: {{ warning.message }} {{ warning.recommendation }}
{% else -%}
- No warnings detected.
{% endfor %}

## Blockers

{% for blocker in result.blockers -%}
- **{{ blocker.code }}**: {{ blocker.message }} {{ blocker.recommendation }}
{% else -%}
- No blockers detected.
{% endfor %}

## Suggested Configuration Summary

{% if result.suggested_config -%}
- App name: `{{ result.suggested_config.app_name }}`
- Namespace: `{{ result.suggested_config.namespace }}`
- Image placeholder: `{{ result.suggested_config.image }}`
- Container port: `{{ result.suggested_config.container_port }}`
- Service port: `{{ result.suggested_config.service_port }}`
{% else -%}
No app.yaml scaffold was produced.
{% endif %}

## Manual Next Steps

1. Review warnings and blockers.
2. Confirm the application can run in a Linux container.
3. Create or review a Dockerfile manually.
4. Replace placeholder image values.
5. Move sensitive settings to an external secret workflow.
6. Validate the generated starter config with `k8s-forge check` before rendering.
